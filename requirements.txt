# requirements.txt for Pi Scanner (Flask Web UI)
# ──────────────────────────────────────────────────────
# Python packages to install via pip
Flask>=2.2
Werkzeug>=2.2
img2pdf>=0.4
tesseract>=0.1.3  # or use pytesseract if preferred
ocrmypdf>=14.0

# System packages (apt-get) needed on Raspberry Pi (documented here for reference)
# sudo apt-get install -y fswebcam tesseract-ocr ocrmypdf imagemagick avahi-daemon uhubctl
